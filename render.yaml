services:
  # Frontend only (demo mode uses localStorage)
  - type: web
    name: david-portfolio
    env: static
    rootDir: packages/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
